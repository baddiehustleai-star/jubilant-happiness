# Example environment variables for Photo2Profit (frontend + functions)
#
# IMPORTANT: Never commit real secret values to the repository. Use .env.local
# (which is gitignored) for local secrets. The placeholders below are safe.

# Firebase
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# Stripe (server-side secret; put your secret key in Vercel or .env.local)
STRIPE_SECRET_KEY=

# Client-visible Stripe price ID (must be prefixed with VITE_ so Vite exposes it)
VITE_STRIPE_PRICE_ID=

# Third-party API keys (optional)
REMOVEBG_API_KEY=
EBAY_APP_ID=
EBAY_CERT_ID=
EBAY_DEV_ID=
EBAY_OAUTH_TOKEN=

# SendGrid (for transactional emails in Cloud Functions)
SENDGRID_API_KEY=

